# 구현 체크리스트

모든 작업은 `specs/<capability>/spec.md`의 Requirement를 근거로 한다. 근거 없는 작업은 범위 이탈이다. 아래는 전부 구현 완료된 상태(신규 작업 착수 시 이 파일에 미완료 항목을 추가한다).

## auth

- [x] 인증 미들웨어가 열린 경로를 통과시킴 (spec: 인증 미들웨어의 열린 경로 통과)
- [x] 유효한 세션 토큰으로 요청 인가 (spec: 유효한 세션 토큰으로 요청 인가)
- [x] 세션 없음/무효 시 401 또는 리다이렉트 분기 (spec: 세션 없음/무효 시 응답 분기)
- [x] 회원가입 사용자명 검증 및 예약어 차단 (spec: 회원가입 사용자명 검증 및 예약어)
- [x] 로그인 레이트리밋 (spec: 로그인 시 레이트리밋)
- [x] 로그인 자격 증명/승인 상태 검증 (spec: 로그인 자격 증명 검증 및 승인 상태 확인)
- [x] 로그인 성공 시 세션 발급·쿠키 설정 (spec: 로그인 성공 시 세션 발급 및 쿠키 설정)
- [x] 로그아웃 시 세션 무효화 (spec: 로그아웃 시 세션 무효화)
- [x] `/api/me` 현재 사용자 조회 (spec: 현재 사용자 조회 (/api/me))

## paper-analyzer

- [x] 히스토리 조회 (spec: 히스토리 조회)
- [x] 논문 검색 (spec: 논문 검색)
- [x] paper_id 또는 URL로 논문 분석 (spec: paper_id 또는 URL로 논문 분석)
- [x] PDF 업로드 분석 (spec: PDF 업로드 분석)
- [x] SJR 저널 품질 조회 (spec: 저널 품질(SJR) 조회)
- [x] PDF에서 제목/초록/식별자 추출 (spec: PDF에서 제목/초록/식별자 추출)
- [x] PDF에서 대표 그림 추출 (spec: PDF에서 대표 그림 추출)

## translator

- [x] 스트리밍 번역 응답 (spec: 번역 스트리밍 응답)
- [x] 캐시 히트 시 즉시 응답 (spec: 캐시 히트 시 즉시 응답)
- [x] 번역 규칙 프롬프트 적용 (spec: 번역 규칙 프롬프트 적용)
- [x] 히스토리 유형 분류 및 저장 (spec: 히스토리 유형 분류 및 저장)
- [x] 번역 히스토리 조회 (spec: 번역 히스토리 조회)

## model-review

- [x] 아키텍처 이미지 기준 설명 생성 (spec: 아키텍처 이미지 기준 설명 생성)
- [x] JSON 전용 응답 강제 및 코드펜스 제거 (spec: JSON 전용 응답 강제 및 코드펜스 제거)
- [x] 사용자 설명 채점 및 교정 (spec: 사용자 설명 채점 및 교정)
- [x] 아키텍처 리뷰 히스토리 조회 (spec: 아키텍처 리뷰 히스토리 조회)

## todo

- [x] Todo CRUD (spec: Todo CRUD)
- [x] 캘린더 지정 시 리마인드 시각 자동 계산 (spec: 캘린더 지정과 리마인드 시각 자동 계산)
- [x] 주간/오늘/메모 필터 조회 (spec: 필터 조회 (주간/오늘/메모))
- [x] Step CRUD (spec: Step CRUD)
- [x] AI 기반 단계 분해 (spec: AI 기반 단계 분해)
- [x] AI 우선순위 전략 제안 — 프론트엔드 UI 제거 이후 미사용, 코드는 의도적으로 유지 (spec: AI 우선순위 전략 제안 (미사용 경로, 유지))
- [x] 주간 리뷰 통계 (spec: 주간 리뷰 통계)
- [x] 마감 리마인드 이메일 스케줄러 (spec: 마감 리마인드 이메일 스케줄러)
- [x] 헬스체크 (spec: 헬스체크)

## contextor

- [x] 단어 조회 (spec: 단어 조회)
- [x] 조회 결과 캐싱 (spec: 조회 결과 캐싱)
- [x] 조회 히스토리 (spec: 조회 히스토리)

## product

- [x] 단일 시각 언어 토큰 적용 (spec: D-001 단일 시각 언어)
- [x] 페이지 계열별 공통 레일 (spec: D-002 페이지 계열)
- [x] 정보 위계 순서 적용 (spec: D-003 정보 위계)
- [x] 공통 페이지 골격 컴포넌트화 (spec: D-004 공통 페이지 골격)
- [x] 타이포그래피·간격 토큰화 (spec: D-005 타이포그래피와 간격)
- [x] 컴포넌트 표면 규칙 적용 (spec: D-006 컴포넌트 표면)
- [x] 아이콘 일관성 (spec: D-007 아이콘)
- [x] 마케팅 이미지 가이드라인 적용 (spec: D-008 이미지)
- [x] 모션 가이드라인 적용 (spec: D-009 모션)
- [x] 반응형 레이아웃 (spec: D-010 반응형)
- [x] 화면별 계약 준수 (spec: 화면별 계약)
- [x] 접근성·i18n 적용 (spec: 접근성과 콘텐츠)

## mobile-navigation

- [x] OS별 하단 네비게이션 컴포넌트 분기 (spec: OS별 하단 네비게이션 컴포넌트 분기)
- [x] Android 하단 바 — 다른 컨테이너, 같은 선택 표시 원칙 (spec: Android 하단 바는 iOS 캡슐 독과 다른 컨테이너, 같은 선택 표시 원칙)
- [x] 세그먼트 폭을 따르는 인디케이터 (spec: 인디케이터는 세그먼트 폭을 따르는 캡슐)
- [x] 데스크톱 탭 클릭 전환 (spec: 데스크톱 탭 전환은 클릭만 지원)
- [x] Plan 탭 Todo/Calendar 통합 (spec: 모바일 하단 독은 5개 슬롯, Plan이 Todo/Calendar 통합)
- [x] 색이 아닌 아이콘 형태로 선택 표시 (spec: 선택 표시는 색이 아니라 아이콘 형태로)
- [x] 눌림 피드백 유지 (spec: 캡슐 눌림 피드백은 손을 뗄 때까지 유지)
- [x] 드래그 중 실시간 추적 (spec: 드래그 중 인디케이터는 포인터를 실시간 추적)
- [x] 러버밴드 엣지 스트레치 (spec: 경계를 넘는 드래그는 반대쪽을 고정한 채 미는 쪽만 늘어남)
- [x] 축소 모션 대응 (spec: 축소 모션 대응)

## 진행 중 / 미해결

- [ ] Android 실기기 검증 — 실기기 없이 UA 오버라이드로만 개발됨. 상세는 `changes/` 대신 이 파일에 직접 남긴다: 실기기(또는 원격 기기 테스트 서비스) 확보 후 바 높이·아이콘 크기·터치 반응 확인, 필요 시 `specs/mobile-navigation/spec.md`를 직접 수정.
